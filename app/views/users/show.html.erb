        <!-- START DIV PAGE-OUTER -->
        <div id="page-outer" >
          <div id="front-container" class="js-front-container">
            <%= render 'layouts/content' %>
            <%= render 'layouts/usattr' %>
            <%= render 'layouts/bottom' %>
          </div>
        </div>
        <!-- END DIV PAGE-OUTER -->
